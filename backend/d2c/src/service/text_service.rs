use helpers::filterlist::FilterList;
use helpers::text_segmenter::TextSegmenter;
pub struct TextService {}

impl TextService {
    fn entities_coordinates(
        segmenter: TextSegmenter,
        filterlist: FilterList,
        entities_list: Vec<String>,
    ) {
    }
}
